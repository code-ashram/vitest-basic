import './style.css'

export const getSumAndProduct = (value: number): void => {
  const numberToString: string = value.toString() // use this variable in your code
  let sum: number = 0
  let prod: number = 1
  // add code below

  // add code above
  console.log(`Sum: ${sum}, product: ${prod}`)
}
